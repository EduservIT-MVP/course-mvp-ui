# Place static .pptx templates here until a PPT agent is integrated.
# The API copies the first *.pptx (or PPT_TEMPLATE_FILE) into each course
# artifact as "{course-slug}-theory.pptx". Source files are never edited.
